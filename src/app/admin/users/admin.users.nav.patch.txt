Add a Users link to the admin sidebar / tab config.

Use this entry in the same array that currently contains:
Overview, Applications, Gallery, Inquiries, Settings

{
  href: "/admin/users",
  label: "Users"
}

If your admin dashboard page uses a tabs array, include:
{ href: "/admin/users", label: "Users" }

If your DashboardShell / admin layout uses navItems, include:
{ href: "/admin/users", label: "Users" }
