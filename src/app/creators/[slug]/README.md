# Creator Dashboard Source Note

No source replacement was generated from the uploaded HTML snapshot.

Use the uploaded rendered HTML under `docs/live-snapshots/` as production verification evidence only. The actual editable Next.js route should remain:

```txt
src/app/creators/[slug]/page.tsx
```

Do not paste rendered HTML into a Next.js App Router page.
