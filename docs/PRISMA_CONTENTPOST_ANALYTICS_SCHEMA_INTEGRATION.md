# Prisma ContentPost Analytics Schema Integration

Updated: 2026-05-05

Historical integration note. Current source of truth is `DATABASE.md`, `PRISMA_MIGRATION_ROLLOUT_CHECKLIST.md`, and Preview smoke testing. Analytics should use additive rollout, direct `ContentPost` fields, and `ContentPostViewSession` while compatibility metadata bridges are retired safely.
