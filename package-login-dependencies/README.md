# RESURGENCE Login Dependencies Update

This package contains the required dependencies for the production login gateway.

## Included files

- src/components/filter-chip-row.tsx
- src/components/profile-completion-meter.tsx
- src/lib/signup-roles.ts

## Apply

Copy the files into the same paths in your repo, then run:

npm run build

If build succeeds:

git add src/components/filter-chip-row.tsx src/components/profile-completion-meter.tsx src/lib/signup-roles.ts
git commit -m "Add login onboarding dependencies"
git push origin main
